using System.Collections.Generic;
using System.Linq;
using API.Models;

namespace API.Dao
{
    public class MySQLDao
    {
        private const string HOST_NAME = "mysql-server";
        private const int PORT_NUM = 9997;

        private const string DATABASE_NAME = "mysql_database";
        private const string DATABASE_USER = "docker";
        private const string DATABASE_PASSWORD = "docker";
        private const string DATABASE_ROOT_PASSWORD = "root";

        // 処理は後ほど
    }
}
